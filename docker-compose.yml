version: "3"
services:
    db:
      image: postgres
      ports:
        - "5432:5432"
      volumes:
        - ./.db:/var/lib/postgresql/data
      environment:
        - POSTGRES_DB=observatorio-femicidios
        - POSTGRES_USER=vivas
        - POSTGRES_PASSWORD=nosqueremos
